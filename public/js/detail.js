function show(id) {
    var item = document.getElementById(id);
    item.classList.add("show");
}

function hide(id) {
    var item = document.getElementById(id);
    item.classList.remove("show");
}
